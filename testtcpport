import socket
HOST=str(raw_input("Qual IP?"))
port=str(raw_input("Qual é a porta TCP"?))
tcp=socket.socket(socket.AF_INET,socket.SOCK_STREAM)
dest=(HOST,port)
tcp.connect(dest)
data = tcp.recv(1024)
print (port, repr(data))
tcp.close()
print("Teste Finalizado")
